<%= form_for(trip) do |f| %>
  <% if trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% trip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :orgin %>
    <%= f.text_field :orgin %>
  </div>

  <div class="field">
    <%= f.label :destination %>
    <%= f.text_field :destination %>
  </div>

  <div class="field">
    <%= f.label :leave_time %>
    <%= f.time_select :leave_time %>
  </div>

  <div class="field">
    <%= f.label :passengers %>
    <%= f.number_field :passengers %>
  </div>

  <div class="field">
    <%= f.label :trip_id %>
    <%= f.text_field :trip_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
